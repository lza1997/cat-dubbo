<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zs.pig.blog.mapper.BlogMapper">

	<sql id="blogBlogColumns">
		id,title,summary,releasedate,clickhit,replyhit,content,typeid,keyword
	</sql>

	<resultMap type="com.zs.pig.blog.api.model.Blog" id="BlogResult">
		<result property="id" column="id"/>
		<result property="title" column="title"/>
		<result property="summary" column="summary"/>
		<result property="releasedate" column="releaseDate"/>
		<result property="clickhit" column="clickHit"/>
		<result property="replyhit" column="replyHit"/>
		<result property="content" column="content"/>
		<result property="keyword" column="keyWord"/>
		
	</resultMap>
	<select id="getLastBlog" parameterType="Long" resultMap="BlogResult">
		SELECT * FROM t_blog WHERE id&lt;#{id} ORDER BY id DESC LIMIT 1
	</select>
	
	<select id="getNextBlog" parameterType="Long" resultMap="BlogResult">
		SELECT * FROM t_blog WHERE id&gt;#{id} ORDER BY id ASC LIMIT 1
	</select>
</mapper>
